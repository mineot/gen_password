// @ts-check

import { DimensionEnum } from "@enums/dimension.enum";
import { TypeEnum } from "@enums/type.enums";

const typeKeys = ["ONLY_NUMBERS", "ONLY_ALPHAS", "ALPHA_NUMERICS", "COMPLEX"];
const typeVals = ["only_numbers", "only_alphas", "alpha_numerics", "complex"];

const dimensionKeys = ["FOUR", "SIX", "EIGTH", "TEN", "TWELVE", "FOURTEEN", "SIXTEEN", "EIGHTEEN", "TWENTY"];
const dimensionVals = [4, 6, 8, 10, 12, 14, 16, 18, 20];

test("Core Type Dimension - Validate Key's", () => {
  // validar o core type dimension
});
